export const path: {
  home?: string;
  catalog?: string;
  product?: string;
  profile?: string;
  favorite?: string;
  view?: string;
  cart?: string;
  'about-us'?: string;
  stocks?: string;
  contacts?: string;
  guarantee?: string;
  credit?: string;
  dropshipping?: string;
  compare?: string;
  orders?: string;
} = {
  home: '/',
  catalog: '/catalog',
  product: '/product/',
  profile: '/profile',
  favorite: '/favorite',
  view: '/view',
  cart: '/cart',
  'about-us': '/about-us',
  stocks: '/stocks',
  contacts: '/contacts',
  guarantee: '/guarantee',
  dropshipping: '/dropshipping',
  credit: '/credit',
  compare: '/compare',
  orders: '/orders',
};
